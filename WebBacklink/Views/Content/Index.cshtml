@model PagedList.IPagedList<Models.EF.Content>

@using PagedList.Mvc;


@{
    ViewBag.Title = "Tin Tức";

    Layout = "~/Views/Shared/_Layout.cshtml";
}
<nav class="navbar" style="background:#4facfe">
    <span class="" style="color:white"><a href="/" style="color:white;"><i class="fas fa-home"></i> Trang Chủ</a> / <a href="/gioi-thieu" style="color:white;">Giới Thiệu</a></span>
</nav>
@*<div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">




                <div class="col-md-6">
                    @foreach (var item in Model)
                    {
                        var link = "/chi-tiet-bao" + item.MetaTitle + "-" + item.ID;
                        <div class="">
                            <div class="">
                                <a href="@link"><img src="@item.Image" alt="@item.MetaKeywords"></a>
                            </div>
                            <div class="">
                                <a href="@link"> <h3>@item.Name</h3></a>
                                <h4>@item.CreatedDate<span>  by:  <a href="#">@item.CreatedBy</a></span></h4>
                                <p style="color:#808080">@item.Decscription <a href="@link" title="more">[....]</a></p>
                            </div>
                        </div>
                    }

                </div>
                <div class="col-md-4 sidebar">
                    <div class="category">


                    </div>

                </div>
            </div>
        </div>

    </div>*@

<div class="container-fluid">
    <div class="row">
        <div class="col-lg-8">
            <div class="row">
                <div class="col-md-12">
                    @foreach (var item in Model)
                    {
                        var link = "/chi-tiet-bao" + item.MetaTitle + "-" + item.ID;
                    <div class="row" style="padding:15px; border-top:1px solid #b9b8b8">


                        <div class="col-lg-4">
                            <a href="@link"><img src="@item.Image" style="width:100%" alt="@item.MetaKeywords"></a>
                        </div>
                        <div class="col-lg-8">
                            <a href="@link"> <h3>@item.Name</h3></a>
                            <h4>@item.CreatedDate<span>  by:  <a href="#">@item.CreatedBy</a></span></h4>
                            <p style="color:#808080">@item.Decscription <a href="@link" title="more">[....]</a></p>
                        </div>

                    </div>
                    }

                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page, @searchString = @ViewBag.SearchString }), new PagedListRenderOptions
               {
                   LiElementClasses = new List<string> { "page-item","page-link" },
                   

               })
                </div>


            </div>

            <!-- Pagination Start -->
            <!-- Pagination Start -->
        </div>

        <!-- Side Bar Start -->
        <div class="col-lg-4 sidebar">
            <div class="sidebar-widget category">
                <nav class="navbar bg-light">

                    @{ Html.RenderAction("Category", "Content");}
                </nav>
            </div>





            <div class="sidebar-widget tag">
                <h2 class="title">Tags Cloud</h2>
                <a href="#">Lorem ipsum</a>
                <a href="#">Vivamus</a>
                <a href="#">Phasellus</a>
                <a href="#">pulvinar</a>
                <a href="#">Curabitur</a>
                <a href="#">Fusce</a>
                <a href="#">Sem quis</a>
                <a href="#">Mollis metus</a>
                <a href="#">Sit amet</a>
                <a href="#">Vel posuere</a>
                <a href="#">orci luctus</a>
                <a href="#">Nam lorem</a>
            </div>
        </div>
        <!-- Side Bar End -->
    </div>
</div>