@model List<Models.EF.About>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="content">
    <div class="content_top">
        <div>
            <p style="font-size:12px;"><a href="/" style="color:white;"><i class="fas fa-home"></i> Trang Chủ</a> >> <a href="/gioi-thieu" style="color:white;">Giới Thiệu</a></p>
        </div>
        <div class="clear"></div>
    </div>
    <div class="container"><h2>Giới Thiệu</h2></div>
    @foreach (var item in Model)
    {
        <div class="container">@Html.Raw(item.Detail)</div>
    }



</div>

