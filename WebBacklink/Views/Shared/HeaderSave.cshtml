@model List<WebBacklink.Models.SaveItem>

<div class="cart">
    <p>Chào Mừng Bạn Đến Với WebSite! </p><a href="/luu-bai-viet">Bài Viết Lưu</a>
    <div id="dd" class="wrapper-dropdown-2">
        @Model.Count Bài Viết 
        @*<ul class="dropdown">
            @foreach(var item in Model)
            {
                var link = "/chi-tiet" + item.Product.MetaTitle + "-" + item.Product.ID;
            <li> <a href="@link">
                
                <p>@item.Product.Name</p>
                 <p>@item.Product.Decscription    </p>
                <img src="@item.Product.Image" width="40" height="40" />
                </a>
            </li>
            }

        </ul>*@
                <ul class="dropdown">
                    @foreach(var item in Model)
                    {
                        var link = "/chi-tiet" + item.Product.MetaTitle + "-" + item.Product.ID;
                    <li class="">
                        <a href="@link" class="header__notify-link" style="display:flex;text-decoration:none;">
                            <span>
                                <img src="@item.Product.Image"
                                     alt="" class="header__notify-img" style="display:flex;width:48px;height:48px;">
                            </span>
                            <div class="header__notify-info">
                                <span class="header__notify-name "style="display:flex;line-height:25px;color:dodgerblue;font-size:20px;padding-left:10px;">@item.Product.Name</span>
                                <span class="header__notify-descirpsopn"style="display:block">@item.Product.MetaDescriptions</span>
                            </div>
                        </a>
                    </li>
                    }
            
            </ul>
    </div><p></p>
</div>
