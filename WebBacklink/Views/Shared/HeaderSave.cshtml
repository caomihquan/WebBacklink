@model List<WebBacklink.Models.SaveItem>

<div class="cart">
    <p>Chào Mừng Bạn Đến Với WebSite! </p><a class="/luu-bai-viet">Bài Viết Lưu</a>
    <div id="dd" class="wrapper-dropdown-2">
        @Model.Count Bài Viết 
        @*<ul class="dropdown">
            @foreach(var item in Model)
            {
            <li></li>
            }
            
        </ul>*@
    </div><p></p>
</div>
